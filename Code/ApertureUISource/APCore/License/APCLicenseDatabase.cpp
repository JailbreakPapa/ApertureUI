#include "APCLicenseDatabase.h"

bool aperture::core::APCLicenseDatabase::CheckLicenseHash(const std::string& keytocheck)
{
  /// Check if license aligns with the compiled license hash & (if trial license) check if it is over a month.
  return false;
}